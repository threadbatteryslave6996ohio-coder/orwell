# The proxy reads the request body itself and replays it upstream verbatim. Spring's form-content
# filter would consume that body first on PUT/PATCH/DELETE with a form content type, leaving the
# upstream with an empty one — so it is off here, unlike in every other app.
spring.mvc.formcontent.filter.enabled=false
