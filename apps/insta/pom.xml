<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>dev.orwell</groupId><artifactId>orwell</artifactId><version>0.1.0-SNAPSHOT</version>
        <relativePath>../../pom.xml</relativePath>
    </parent>
    <artifactId>insta</artifactId>
    <properties>
        <mainClass>dev.orwell.insta.InstaCli</mainClass>
    </properties>
    <dependencies>
        <dependency><groupId>dev.orwell</groupId><artifactId>env-core</artifactId></dependency>
        <dependency><groupId>dev.orwell</groupId><artifactId>logger</artifactId></dependency>
        <!-- The shared Redis handle, same as keeboarder-server: one Redis client in the repo, not two. -->
        <dependency><groupId>dev.orwell</groupId><artifactId>redis-client</artifactId></dependency>
        <!-- The graph viewer. The repo's lightweight HTTP runtime: three endpoints and a page do
             not need a Spring container, and this app deliberately does not have one. -->
        <dependency><groupId>dev.orwell</groupId><artifactId>undertow-bootstrap</artifactId><version>${project.version}</version></dependency>
        <dependency><groupId>dev.orwell</groupId><artifactId>primitives</artifactId></dependency>
        <dependency><groupId>com.fasterxml.jackson.core</groupId><artifactId>jackson-databind</artifactId></dependency>
        <!-- Posts carry an Instant, and the cache round-trips profiles through JSON. -->
        <dependency><groupId>com.fasterxml.jackson.datatype</groupId><artifactId>jackson-datatype-jsr310</artifactId></dependency>
        <!-- The graph store. Plain JDBC: one connection per sync does not want a pool or an ORM. -->
        <dependency><groupId>org.postgresql</groupId><artifactId>postgresql</artifactId><scope>runtime</scope></dependency>
        <!-- Jedis drags in slf4j-api, which prints a three-line "no providers found" warning on
             every single invocation of a command-line program. This binds it to nothing. Repo code
             still logs through dev.orwell.logging.Logger; this only silences the transitive noise. -->
        <dependency><groupId>org.slf4j</groupId><artifactId>slf4j-nop</artifactId><scope>runtime</scope></dependency>
        <dependency><groupId>org.junit.jupiter</groupId><artifactId>junit-jupiter</artifactId><scope>test</scope></dependency>
        <dependency><groupId>org.assertj</groupId><artifactId>assertj-core</artifactId><scope>test</scope></dependency>
        <dependency><groupId>org.testcontainers</groupId><artifactId>junit-jupiter</artifactId><scope>test</scope></dependency>
        <dependency><groupId>org.testcontainers</groupId><artifactId>postgresql</artifactId><scope>test</scope></dependency>
    </dependencies>
    <build>
        <plugins>
            <!-- A plain program, so the runnable artifact is the shaded insta-<version>-exec.jar,
                 the same shape as klippy-file-locker. There is no Spring context to repackage. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <configuration>
                    <shadedArtifactAttached>true</shadedArtifactAttached>
                    <shadedClassifierName>exec</shadedClassifierName>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
